# .trae/documents —— 归档说明

本目录存放的是实现过程中的**过程草稿/方案草稿**，多已落地为功能。
**正式文档一律以 [`doc/设计文档`](../../doc/设计文档/) 为准**（正文均实现「现行」描述），
权威索引见 [`doc/README.md`](../../doc/README.md)。

各草稿对应的正式文档，按主题可在上述索引的「一、设计文档」一节定位；状态以索引标注为准，本目录不再维护。

> 约定：新写方案请直接放 `doc/设计文档/`；如仍需在 .trae 下留工作稿，完成后在此登记去向或删除。